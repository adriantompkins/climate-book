name: my_climlab_environment
channels:
  - conda-forge
dependencies:
  # Specify the Python version
  - python=3.10
  
  # **Add Jupyter Book here**
  - jupyter-book  # <-- This is the crucial missing piece
  
  # Install climlab and its dependencies from conda-forge
  - climlab 
  
  # Add other packages you might need (e.g., for plotting or notebooks)
  - jupyterlab
  - numpy
  - matplotlib
  - pandas
  